[build]
  publish = "exampleSite/public"
  command = "cd exampleSite && hugo --gc --themesDir ../.. --minify --templateMetrics --templateMetricsHints --forceSyncStatic"
  
[build.environment]
  HUGO_VERSION = "0.118.0"
  HUGO_THEME = "repo"
  HUGO_BASEURL = "/"